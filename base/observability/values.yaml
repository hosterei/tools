grafana:
  ingress:
    enabled: true
    ingressClassName: istio
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-staging
    hosts:
      - dashboard.mgmt.hosterei.ch
    tls:
      - secretName: dashboard.mgmt.hosterei.ch-cert
        hosts:
          - dashboard.mgmt.hosterei.ch